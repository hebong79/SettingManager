# ParkAgent 프로젝트 코딩 규칙
"당신은 REST API 및 Agentic AI, NodeJs(TypeScript), 파이썬 전문가 입니다. 반드시 유닛테스트를 진행 해주고, 실제 테스트를 거쳐 정상적인 동작을 확인해줘. 그리고 생성된 클래스 및 수정된 내용의 설명을 md파일로 만들어줘(한글로 생성) , 주변 파일에 영향이 있는지도 분석해줘."

1. **설계 필수**: 모든 코드 변경 및 신규 기능 구현 시 먼저 설계(계획)을 한다.
2. **유닛 테스트 필수**: 모든 코드 변경 및 신규 기능 구현 시 유닛 테스트를 작성하고 실행하여 검증한다.
3. **동작 확인**: 단순히 코드만 작성하는 것이 아니라, 실제 테스트 환경(Edit Mode/Play Mode)에서 정상 동작함을 확인한다.
4. **상세 문서화**: 변경 사항, 신규 클래스, 수정 로직에 대해 한글 `.md` 파일로 상세히 기록한다. ( yyyyMMdd_hhmmss_이름.md 형식의 파일이름) 
5. **영향도 분석**: 수정 사항이 프로젝트의 다른 부분(어셈블리, 의존성, 기존 기능 등)에 미치는 영향을 분석하여 보고한다.

## 메모 정본: `memo/memo.md` (단일 통일)

- **"메모 해줘"류 요청의 기록 정본은 프로젝트 `memo/memo.md` 한 곳이다.** 홈 auto-memory(MEMORY.md)나 다른 파일에 이중 기록하지 않는다(2026-07-23 마스터 결정).
- **세션 시작 시 `memo/memo.md`(및 `memo/INDEX.md`)를 읽어** 이전 맥락을 잇는다. memo.md 는 자동 로드되지 않으므로 이 규칙으로 참조를 보장한다.
- 기록 형식·역순·1MB 회전 정책은 `memo/memo.md` 상단 사용 안내를 따른다. 긴 작업 **중간** 복구 메모는 `checkpoint` 스킬 담당(역할 구분).
- auto-memory(MEMORY.md)는 이제 memo.md 로 유도하는 포인터 역할만 한다(경쟁 저장 금지).

## 하네스: SettingManager 개발

**목표:** 위 5대 규칙(설계→유닛테스트→동작확인→한글문서화→영향도분석)을 4인 에이전트 팀으로 강제 실행한다.

**트리거:** 이 저장소의 코드 구현·수정·부트스트랩·테스트 요청 시 `settingmanager-dev` 스킬을 사용하라. 단순 조회·질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-31 | 초기 구성 — 4인 팀(architect/developer/qa-tester/documenter) + `settingmanager-dev`(오케스트레이터) + `settingmanager-stack`(TS ESM·vitest 규약) | 전체 | - |







