# SettingManager Hermes 하네스

이 파일은 Hermes가 이 저장소에서 자동으로 읽는 최우선 프로젝트 규칙이다. Claude의 `CLAUDE.md` 및 `.claude/` 하네스와 동일한 목표·산출물·검증 기준을 Hermes 방식으로 적용한다.

## 트리거

SettingManager의 코드 구현·수정·버그 수정·리팩터링·부트스트랩·테스트 추가·SettingAgent 연동 요청이면, 먼저 Hermes 스킬 `settingmanager-dev`를 로드하고 그 파이프라인을 끝까지 따른다. 단순 조회·질문은 파이프라인 없이 답할 수 있다.

## 강제 5대 규칙

1. 코드 변경 전에 설계와 검증 가능한 성공 기준을 만든다.
2. 모든 변경에 유닛 테스트를 작성하고 실제로 실행한다.
3. 가능한 실제 환경에서 동작을 확인한다. 불가능하면 미검증 사유를 명확히 남긴다.
4. 실제 변경·검증 결과를 바탕으로 `docs/yyyyMMdd_hhmmss_이름.md` 형식의 한글 문서를 작성한다.
5. 모듈·의존성·기존 기능·형제 프로젝트에 미치는 영향을 구체적 파일 경로와 함께 분석한다.

## 4역할 순차 파이프라인

`architect → developer → qa-tester → documenter` 순서로 진행한다. 각 역할의 산출물은 `_workspace/`에 보존하고, 다음 역할이 반드시 앞 단계의 산출물과 실제 코드를 확인한다.

1. architect: `_workspace/01_architect_plan.md` — 범위, 가정/확인 필요, 단계별 성공 기준, 영향 파일, 비범위를 설계한다. 코드를 수정하지 않는다.
2. developer: 계획 범위 안에서만 구현하고 `_workspace/02_developer_changes.md`에 변경 파일, 근거, 경계면을 기록한다.
3. qa-tester: Vitest 테스트를 작성·실행해 `_workspace/03_qa_report.md`에 통과/실패/스킵, 기준 대조, 생산자·소비자 경계면 비교, 미검증 사유를 기록한다. 실패하면 구현의 원인만 고치고 재실행한다. 같은 결함이 3회 반복되면 중단하고 근거와 선택지를 보고한다.
4. documenter: 실제 코드 및 01~03 산출물만 근거로 한글 문서와 `_workspace/04_doc_impact.md`를 작성한다.

같은 파일을 변경할 수 있는 구현은 직렬로 진행한다. 읽기·탐색·독립 검증만 병렬화한다.

## 역할별 모델 고정 (필수)

기본 `delegate_task`는 역할별 모델 선택을 지원하지 않으므로 이 하네스에는 사용하지 않는다. 역할 에이전트는 반드시 독립 Hermes 프로필/프로세스로 실행하며, `scripts/run-hermes-role.ps1`가 프로필과 `--model` 인자를 함께 고정한다.

| 역할 | 프로필 | 고정 모델 |
|---|---|---|
| architect(설계) | `settingmanager-architect` | `gpt-5.6-sol` |
| developer(개발) | `settingmanager-developer` | `gpt-5.6-terra` |
| qa-tester(검증·테스트) | `settingmanager-qa` | `gpt-5.6-terra` |
| documenter(문서화·영향도) | `settingmanager-documenter` | `gpt-5.6-luna` |

프로필을 처음 만들거나 모델을 복구할 때는 Windows PowerShell에서 `scripts/setup-hermes-role-profiles.ps1`를 실행한다. 역할 실행은 `scripts/run-hermes-role.ps1 -Role <역할> -Task '<작업 요청>'`만 사용한다. 이 스크립트는 해당 역할 프로필과 모델을 이중으로 검증·고정한다.

## 시작 시 확인

- `memo/memo.md`, 존재한다면 `memo/INDEX.md`, 기존 `docs/`, 기존 `_workspace/`를 읽는다.
- `_workspace/`가 있고 새 주제이면 기존 폴더를 실제 시각 기반 `_workspace_prev_yyyyMMdd_HHmmss/`로 보관한 후 새 파이프라인을 시작한다.
- `SettingMain/package.json`이 없으면 설계 첫 단계에 부트스트랩을 넣고 `settingmanager-stack` 스킬을 따른다.
- 형제 `AgentVLA/ParkAgent/SettingAgent` 연동 계약은 실제 코드·문서로 확인하기 전까지 추측하지 않는다. 불명확하면 `확인 필요:`로 사용자에게 알린다.
- “메모 해줘”의 정본은 `memo/memo.md` 하나다. 설계·구현 문서는 `docs/`, 메모는 `memo/`에만 기록한다.

## 최종 보고 필수 항목

변경 파일, 실제 테스트 통과/실패/스킵 수, 생성 문서 경로, 형제 프로젝트를 포함한 영향도, 미완·미검증 항목과 사유를 숨김없이 보고한다.
