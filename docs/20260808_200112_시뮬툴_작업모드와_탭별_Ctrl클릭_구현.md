# 시뮬레이터 툴 — 「작업모드」와 탭별 Ctrl+클릭 (1차)

- 일시: 2026-08-08 20:01:12
- 브랜치: `feat/simtool-workmode-ctrl-click` (main `fbcaac4` 에서 분기)
- 워크스페이스: `_workspace/simtool-workmode/`
- 상태: **구현·유닛테스트·라이브 실동작 검증 완료** — 단 §6-4 의 두 가지는 확인하지 못했다

---

## 1. 마스터 지시와 결정

> 2. 각 탭에서 작업모드를 클릭했을때는 **플래그를 통해 키보드 제어권**이 각 탭의
>    오브젝트(선택된 오브젝트)를 이동, 회전할수 있게.
> 3. 프리셋 메이커 : **ctrl+왼쪽클릭 = 프리셋오브젝트를 클릭위치로 이동**,
>    프리셋리스트 목록을 클릭하면 화면의 프리셋 오브젝트 선택됨
> 4. 차량배치 : **ctrl+왼쪽클릭 = 차량 추가**, 그냥 클릭 = 차량선택
> 5. 카메라 컨트롤: **ctrl+왼쪽클릭 = 카메라 위치 이동**(+폴대위치 이동)

착수 전 세 가지를 물어 결정을 받았다.

| 질문 | 결정 | 근거 |
|---|---|---|
| main 의 커밋 안 된 이전 작업 | **먼저 커밋**하고 분기 | 새 작업의 diff 가 순수해진다 |
| 작업모드 플래그의 범위 | **키보드 + 마우스 둘 다** | 실수로 Ctrl+클릭해 시뮬레이터가 바뀌는 일이 없다 |
| 전역 「영상 클릭」 드롭다운 | **제거** | 지시 3~5 가 클릭을 탭별로 재정의 — 정본이 둘이면 안 된다 |

---

## 2. 무엇이 문제였나

지금까지 클릭 의미는 **뷰 옆의 전역 드롭다운**(쓰지 않음/차량 선택/차량 배치)이 정했다.
지시 3~5 는 그것을 **탭별**로 재정의한다 — 같은 왼쪽 클릭이 프리셋 탭에서는 프리셋 이동,
차량 탭에서는 차량 선택이다. 둘을 함께 두면 **클릭 의미의 정본이 둘**이 되고, 화면 안내는
그 곱(3모드 × 5탭)을 설명해야 한다.

키보드도 같다. WASD 는 **항상 메인 뷰 시점**을 움직였다. 오브젝트를 움직이려면 누군가
소유권을 넘겨받아야 하고, **그 넘겨받음이 눈에 보여야 한다** — 안 보이면 W 를 눌렀는데
시점이 안 움직이는 것이 고장으로 읽힌다.

---

## 3. 설계

### 3-1. 작업모드 = 소유권 플래그 하나

탭마다 체크박스 하나(`spWork`·`carWork`·`camWork`). 켜지면 **그 탭이 키보드와 Ctrl+클릭을
가져간다.** 활성 탭의 것만 물어보므로 둘이 켜져도 충돌하지 않는다.

| | 작업모드 OFF | 작업모드 ON |
|---|---|---|
| W A S D | 메인 뷰 시점 이동 (기존) | **선택된 오브젝트** 평면 이동 |
| Q E | — | **선택된 오브젝트** 회전 |
| Ctrl + 왼쪽 클릭 | — | 탭별 동작 |
| 왼쪽 클릭 | — | 차량 탭만: 선택 |

**메인 뷰 안내가 누가 키보드를 갖고 있는지 말한다.** 이것이 없으면 "WASD 가 안 먹는다"가
된다 — 화면에 아무 표시가 없는 소유권 이전은 고장과 구별되지 않는다.

### 3-2. ⚠ 이동 축은 **월드**다 — 화면 기준이 아니다

`W` 를 「화면에서 위쪽」으로 만들려면 메인 뷰의 yaw 로 축을 돌려야 한다. 그 삼각함수가
화면에 들어오는 순간 **카메라 규약이 두 벌**이 되고, 그 실패는 오류로 뜨지 않는다 —
오브젝트가 조금 엇나간 쪽으로 갈 뿐이다. 이 저장소는 그것을 이미 테스트로 막고 있고
(화면 스크립트에 `Math.(tan|atan2?|cos|sin)` 금지), 메인 뷰 WASD 를 서버로 넘긴 것도
같은 이유였다.

그래서 **기존 방향 패드와 같은 월드 축**을 쓴다 — `W=+Y · S=−Y · D=+X · A=−X`.
프리셋 탭의 ▲▼◀▶ 와 정확히 같은 뜻이라 사람이 두 규약을 외우지 않는다.

### 3-3. 공용 모듈로 둔 이유

세 탭이 같은 규율을 필요로 한다. 특히 **키 반복 억제**는 빠뜨려도 오류로 뜨지 않고
"손을 뗐는데 계속 간다"로만 나타난다 — 메인 뷰 WASD 가 이미 겪은 실패다. 세 벌로 두면
한 곳만 고치는 날이 온다. 그래서 `web/simtoolWorkMode.js` 가 체크박스·스텝·키 매핑·중복
억제를 갖고, 탭은 「대상이 무엇인가」와 「어떻게 옮기는가」만 준다(파일 막대와 같은 판단).

### 3-4. ⚠ 카메라 탭만 이동과 회전의 **단위가 다르다**

이동은 미터, 회전(pan)은 도(°)다. 한 칸을 나눠 쓰면 **2° 를 돌리려던 사람이 카메라를 2m
옮긴다** — 오류로 뜨지 않고 카메라만 엉뚱한 자리에 간다. 그래서 입력칸을 나눴다:
`camMoveStep`(m) 과 기존 `camStep`(°). 프리셋·차량은 방향 패드가 이미 한 칸을 m 이자 °
로 쓰고 있어 그 규약을 따랐다.

*(구현 도중 공용 모듈이 step 을 곱해 넘기게 만들었다가 이 함정을 만들었다 — 계약을
`rotate(target, sign)` 으로 바꿔 각도 단위를 탭이 정하게 했다.)*

### 3-5. 프리셋 목록 클릭은 **작업모드일 때만** 시뮬레이터를 두드린다

이 목록은 **파일**에서 온 것이고 시뮬레이터의 프리셋과 **다른 축**이다. 파일에 6건이 있고
시뮬레이터가 비어 있으면 목록을 훑는 것만으로 매번 오류가 난다. 작업모드가 "지금 나는
시뮬레이터를 몰고 있다"는 선언이므로 그때만 부른다. 실패는 **삼키지 않고 말한다** —
고른 줄 알고 키보드를 누르면 엉뚱한 것이 움직인다.

### 3-6. Ctrl+클릭은 **절대**, 키보드는 **상대**

`preset.move` 는 둘 다 받는다(`to` / `delta`). Ctrl+클릭이 상대를 쓰려면 지금 위치를 알아야
하는데 그 값은 시뮬레이터가 갖고 있다 — 한 번 더 묻는 사이에 누가 옮기면 어긋난다.
"여기로 가라"가 짧고 안전하다.

**높이(z)는 보내지 않는다.** 클릭이 맞힌 것은 지형이고 프리셋의 높이는 사람이 정한
Offset Z 라, 여기서 덮어쓰면 「상세」의 값과 시뮬레이터가 조용히 갈린다.
카메라도 같은 이유로 **높이를 지킨다** — 지면 z 를 쓰면 카메라가 바닥에 처박힌다.

### 3-7. 더블클릭 조준의 조건이 바뀌었다

브라우저는 `dblclick` 을 클릭 두 번 **뒤에** 준다. 전에는 「영상 클릭」이 「쓰지 않음」일
때만 받았지만, 드롭다운이 없어졌으므로 **Ctrl 을 누르지 않았을 때만** 받는다 —
Ctrl 없는 클릭은 「선택」뿐이라 겹쳐도 무해하다.

---

## 4. 바꾼 파일

| 파일 | 구분 | 내용 |
|---|---|---|
| `web/simtoolWorkMode.js` | **신규** | 작업모드 공용 — 플래그·키 매핑·중복 억제·안내 |
| `web/simtoolPreset.js` | 수정 | 작업모드 + Ctrl 클릭 절대이동 + 목록클릭 `preset.select` |
| `web/simtoolCar.js` | 수정 | 작업모드 + Ctrl=추가 / 클릭=선택 (`placeAt`·`selectAt` 로 분리) |
| `web/simtoolCam.js` | 수정 | 작업모드 + Ctrl=카메라 위치 이동 (`moveTo`) |
| `web/simtool.js` | 수정 | 드롭다운 제거, 클릭을 활성 탭으로 분기, 키보드 라우팅 |
| `web/simtool.html` | 수정 | 작업모드 UI 3개, 드롭다운 제거, 낡은 안내 갱신 |
| `test/simtoolUi.test.ts` | 수정 | 신규 29건 + **낡은 사실을 못 박던 2건 갱신** |

**서버(`src/`)는 한 줄도 바꾸지 않았다.** 쓰는 RPC 가 전부 이미 카탈로그에 있고
(`preset.move`·`preset.rotate`·`preset.select`·`car.setPosition`·`car.setRotationY`·
`car.select`·`cam.setPosition`·`cam.setPTZ`), 통로는 기존 `/api/sim/rpc` 다 →
**새 라우트가 없으므로 MCP 드리프트 무관**, `routeCatalog.ts` 변경 없음.

---

## 5. 검증

### 5-1. 유닛테스트

```
test/simtoolUi.test.ts   63 → 92건 (+29)   전부 통과
tsc --noEmit                                exit 0
전체 회귀 1075건 중 20 실패
  → 변경 전(1035건 중 20 실패)과 **동일한 3개 파일·동일한 20건**
  → stash 후 재실행해 같은 20건임을 확인함 (내 변경과 무관)
  → 늘어난 40건은 전부 이번 신규이고 전부 통과
```

신규 테스트가 막는 실패:

| 테스트 | 막는 실패 |
|---|---|
| 전역 클릭 모드 부재 | 같은 클릭에 두 규칙이 남아 **어느 것이 이겼는지 모르는** 상태 |
| 키보드는 활성 탭이 **먼저** | 한 번 누를 때 오브젝트와 시점이 **함께** 움직이는 것 |
| `busy` 가드 존재 | 키 반복으로 요청이 쌓여 **손을 뗀 뒤에도 계속 가는** 것 |
| 월드 축 (삼각함수·`yaw` 금지) | 카메라 규약이 두 벌이 되는 것 |
| 메인 뷰 안내가 소유권을 말함 | "WASD 가 안 먹는다"가 고장으로 읽히는 것 |
| `camMoveStep` ≠ `camStep` | **2° 를 돌리려다 2m 옮기는** 것 |
| 폴대 RPC 를 지어 부르지 않음 | 카탈로그 400 으로 위치 이동까지 실패하는 것 |
| 프리셋 `to` 에 z 없음 | 지형 높이가 Offset Z 를 덮어써 화면과 갈리는 것 |
| 카메라가 `pos.z` 를 지킴 | 카메라가 **바닥에 처박히는** 것 |
| 목록 클릭이 `work.isOn()` 게이트 | 파일 목록을 훑는 것만으로 오류가 쏟아지는 것 |

### 5-2. 라이브 실동작 (시뮬레이터 13510 · Chrome)

**숫자를 화면에서만 보지 않았다** — 조작 뒤 시뮬레이터에 직접 물어 대조했다.

| 항목 | 결과 |
|---|---|
| 모듈 로딩 | 다섯 탭 생존 · **연결됨 86 method** · 콘솔 오류 없음(404 는 favicon) |
| 드롭다운 제거 | `simClickMode` **DOM 에 없음** |
| 안내가 탭을 따라감 | 프리셋/차량/카메라 탭에서 `simClickNote` 가 각각 다르게 표시됨 |
| **차량 — 그냥 클릭** | `47-09.48.44` 선택. `view.pick(566,290).actorId` 와 **일치** |
| **차량 — Ctrl+클릭** | `65-19.57.06` 을 (−11.24, −14.68) 에 추가, 65 → **66대**(목록만) |
| **차량 — 작업모드 W/D/E** | 화면 (3.371,2.061)→(3.871,2.561) rotY 0→0.5 |
| ↳ 시뮬레이터 실측 | **(3.870518, 2.561410, 0) rotY 0.5 — 정확히 일치** |
| ↳ **메인 뷰는 안 움직였다** | pos·rot·fov 시작값 그대로 ✅ 소유권 이전 확인 |
| **작업모드 OFF 후 W** | 메인 뷰 (−20.4,−1.6,20.9) → **(−19.6,−1.7,20.3)**, 차량은 그대로 ✅ 반대 방향도 확인 |
| **카메라 — Ctrl+클릭** | #1 을 (8.99, −25.68) 로. 실측 **(8.9855, −25.6752, 13.5)** — z **13.5 유지** ✅ |
| **카메라 — 작업모드 W/D** | (8.99,−25.68) → **(9.49,−25.18)**, z **13.50 그대로** |
| **카메라 — E (회전)** | pan 47.1 → **49.1 (+2°)** — `camMoveStep` 0.5 가 **아니라** `camStep` 2 ✅ §3-4 확인 |
| **프리셋 — 목록 클릭** | `preset.select {idx:2}` 발신 (작업모드 ON 일 때만) |
| **프리셋 — 작업모드 W** | `preset.move {idx:2, delta:{x:0, y:0.5}}` 발신 |
| **프리셋 — Ctrl+클릭** | `view.pick` → `preset.move {idx:2, to:{x:−35.598, y:−12.276}}` — **z 없음** ✅ |
| ↳ 시뮬레이터 응답 | `프리셋 없음: idx=2` → **화면이 그대로 표시**(삼키지 않음) — §6-4 참조 |

발신 요청은 화면 안내가 아니라 `fetch` 를 가로채 **요청 본문 자체**를 확인했다.

### 5-3. 검증 뒷정리

시뮬레이터를 **시작 상태로 되돌리고 확인**했다.

- 차량 `47-09.48.44` → pos(3.370518, 2.061410, 0) rotY 0 · 총 **65대**
- 카메라 #1 → pos(−36.3, −13.6, 13.5) · pan 47.1 tilt 30.4 zoom 2.4
- 메인 뷰 → pos(−20.387, −1.571, 20.934) rot(pitch −39.223, yaw −6.300) fov 94
- 영상 스트림 정지(슬롯 반납 — `slots:1` 이라 다투면 남의 화면이 안 나온다)
- 13030 마스터 서버는 끄지 않았다

---

## 6. 주변 영향 분석

### 6-1. 서버·독립 경계

- **`src/` 무변경.** 새 라우트가 없어 `routeCatalog.ts`·MCP 드리프트 테스트 무관.
- `test/simIndependence.test.ts` 무관 — `src/sim/` 의 import 규칙을 안 건드렸다.
- 화면은 여전히 `/api/sim/*` 뿐이다. 영상 소스 하나의 예외도 그대로다.

### 6-2. 거리 측정 · 조명 탭

**변경 없다.** `clickHelp`·`onKey` 를 두지 않았으므로 클릭은 조용히 넘어가고 키보드는
메인 뷰로 간다. 화면이 "이 탭은 아직 클릭을 받지 않습니다"라고 **그대로 말한다.**

### 6-3. 기존 조작은 전부 살아 있다

방향 패드·PTZ 패드·슬라이더·파일 막대·「시뮬로 보내기」는 손대지 않았다. 오른쪽 버튼
회전·휠 화각·더블클릭 조준도 그대로다(더블클릭 조건만 §3-7 로 바뀌었다).

### 6-4. ⚠ 확인하지 못한 것 둘 — **되는 척하지 않는다**

**① 프리셋 이동이 실제로 되는지는 확인 못 했다.**

라이브 시뮬레이터의 `preset.list` 가 **비어 있고** `preset.get {idx:1}` 이
`프리셋 없음` 을 준다 — RPC 가 만든 프리셋이 하나도 없다(위젯이 그린 것은 별개 목록이라
`preset.*` 에 안 보인다 — 기존 문서 §5 의 그 사실 그대로다).

배선은 §5-2 에서 요청 본문까지 확인했다. **`preset.create` 로 임시 프리셋을 만들면
끝까지 확인할 수 있지만, 탭의 경고 배너가 「`preset.*` 를 호출하면 위젯이 그려 둔 주차면이
사라진다」고 적고 있어** 마스터 씬을 훼손할 수 있다. 그래서 하지 않았다 — **허락을 받고
확인해야 한다.**

**② 「+폴대위치 이동」은 확인 못 했다.**

- 등록된 86개 method 중 `pole`·`mast`·`rig`·`tower`·`pillar` 에 걸리는 것이 **하나도 없다.**
- 카메라를 옮긴 뒤 그 자리를 메인 뷰로 비춰 봤지만 **폴대도 카메라 메시도 보이지 않았고**,
  **원래 자리(−36.3, −13.6)에도 아무것도 없었다.** 라인트레이스(`view.pick`)도 두 곳 모두
  지면(z≈0)만 맞히고 `actorId` 를 주지 않는다.

즉 이 빌드의 PTZ 카메라는 **메인 뷰에 렌더되는 몸체가 없다.** 그래서 "폴대가 따라왔는지"를
바깥에서 판정할 방법이 없다. 화면에는 「폴대는 카메라 액터에 붙어 `cam.setPosition` 이 함께
옮깁니다」라고 적어 두었지만 **이것은 전제이지 확인된 사실이 아니다** —
폴대가 별도 액터라면 `view.*` 때처럼 **언리얼 쪽 신설**이 필요하다.

---

## 7. 남은 것

1. **§6-4 ① 프리셋 이동 end-to-end 확인** — 임시 프리셋 생성 허락 필요.
2. **§6-4 ② 폴대** — 언리얼에 폴대가 별도 액터인지 확인. 별도면 `cam.setPosition` 이
   함께 옮기도록 고치거나 `cam.setPolePosition` 신설.
3. **거리 측정 탭은 여전히 클릭을 받지 않는다** — `measure.setTargetPoint` 에 픽 좌표를
   넘기면 되지만 이번 지시 범위 밖이다. 화면이 그렇다고 적어 두었다.
4. **위아래(Z) 전용 키는 두지 않았다** — 지시에 없었다. 필요하면 `R`/`F` 를 더한다.
5. **작업모드는 탭을 옮겨도 켜진 채로 남는다** — 체크박스가 그 탭에 보이므로 숨은 상태가
   아니고, 활성 탭의 것만 물어보므로 다른 탭에 영향이 없다.
